const colors = {
  // Main background — very light warm off-white/beige (makes clothing photos stand out)
  background: "#FDFBF7",          // Soft warm ivory/off-white (primary page bg)

  // Cards, sections, product containers — slightly deeper neutral for depth/hierarchy
  container: "#F8F5F0",           // Light warm beige/cream (for product cards & sections)

  // Main body text & headings — dark charcoal/near-black for high contrast & elegance
  primaryText: "#1A1A1A",         // Deep charcoal black (headlines, product names)

  // Secondary text (descriptions, prices, labels, breadcrumbs) — softer gray
  secondaryText: "#5C5C5C",       // Medium-dark warm gray (subtle, readable)

  // Accent color — warm muted gold/amber (for prices, "Add to Cart", "Show more", hovers, highlights)
  accent: "#C19A6B",              // Muted caramel/gold (luxury CTA & price emphasis)

  // Borders, dividers, subtle lines — very light gray/beige for minimal separation
  border: "#E5E0D9",              // Soft warm gray-beige (cards borders, hr lines)

  // Body/inline text, icons — slightly lighter dark than primary for better flow
  text: "#2E2E2E",                // Dark gray (general paragraph text)

  // Optional extras for completeness (common in fashion e-com sites)
  neutralLight: "#FAF9F6",        // Almost pure white (for hover states or highlights)
  error: "#A83232",               // Muted deep red (alerts, out-of-stock)
  success: "#4A7043",             // Subtle olive green (added to cart, success messages)
  linkHover: "#B58A5A",           // Slightly darker accent variant (for active/hover states)
};

export default colors;