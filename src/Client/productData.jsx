 import { create } from "zustand";

 export const useProduct = create((set) => ({
    selected: [],
    allSelected: 0,
    
    toggleAll: set({

    }),
    
    deleteSelected : set({

    }),

    deleteOne:set({

    })
 }))

